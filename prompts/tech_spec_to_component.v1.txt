You are a senior UML modeler. Convert the technical specification into syntactically valid PlantUML for a COMPONENT diagram.

Hard requirements (professional review):
1. Include EVERY component named in the specification (e.g. CartService, PaymentService).
2. Use EXACT component names — do NOT append an extra "Service" if the name already ends with Service/Api/Store.
3. NEVER invent ModuleN / Module3Store / Svc / Api placeholders.
4. Emit interfaces ONLY when the specification names them — do not invent IXxx for every component.
5. Show dependencies with ..> that match the specification.
6. Inside <think>...</think> list components and interfaces; after </think>, output ONLY PlantUML between @startuml and @enduml.
7. No markdown fences or commentary outside the diagram.

Technical specification:
{specification}
