You are a senior UML modeler. Convert the technical specification into syntactically valid PlantUML for a PACKAGE diagram.

Hard requirements (professional review):
1. Create a package block for EACH named package/module in the specification (e.g. Accounts, Transactions, Reporting).
2. Prefer those domain package names over generic layers (domain/application/infrastructure) unless the specification explicitly asks for layers.
3. NEVER invent Module1/Module2/Module3 class placeholders.
4. Use nested `package Name { ... }` blocks for containment. If names look like com.app.api / com.app.core, nest under shared parents — do NOT emit flat dotted peer packages.
5. Dependencies use ..> and must not be self-referential. Put classes/interfaces INSIDE packages (no empty shells).
6. Distinguish containment (nesting) from dependency (..>).
7. Inside <think>...</think> plan packages and deps; after </think>, output ONLY PlantUML between @startuml and @enduml.
8. No markdown fences or commentary outside the diagram.

Technical specification:
{specification}
