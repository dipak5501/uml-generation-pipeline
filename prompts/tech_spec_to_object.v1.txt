You are a UML expert. Convert the technical specification into syntactically valid PlantUML for an OBJECT diagram.

Chain-of-Thought (required):
1. Inside <think>...</think>, identify concrete instances (name:Type), attribute values, and links.
2. After </think>, output ONLY final PlantUML between @startuml and @enduml.
3. Use object instances with name:Type syntax.
4. No markdown fences or commentary outside the diagram.

Technical specification:
{specification}
